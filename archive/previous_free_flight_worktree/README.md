# Previous Free-Flight Worktree Archive

This directory preserves the custom free-flight work that was originally developed in:

- `/home/hht/PteraSoftware_free_flight`

It contains:

- the legacy exploratory example script
- all diagnostic plots
- all movies and WebP animations
- all search CSV/JSON outputs
- all long-run summaries

Current branch-local production cases now live in:

- `examples/free_flight_gliding_wing.py`
- `examples/free_flight_flapping_forward.py`
- `output/free_flight_cases/`

This archive exists so the old worktree can be removed later without losing the original exploratory artifacts.
